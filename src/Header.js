import React from 'react';

function Header(){
    return(
    <header className="App-header">
      <h1 className="ui centered header">Weather Dashboard</h1>
    </header>
    )
}

export default Header;